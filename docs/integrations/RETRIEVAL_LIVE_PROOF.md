# CML Retrieval live proof

This temporary pull request validates the installed CML Memory Retrieval workflow on `main`.

The change intentionally mentions GitHub Actions workflow permissions, an exact generated branch, pull-request fallback, deterministic memory ranking, privacy filtering, and managed-comment evidence so a related accepted public memory can match when one is available.

The pull request will be closed without merge after the workflow creates or updates one `Relevant CML Memory` comment and uploads its exact-attempt evidence artifact.
