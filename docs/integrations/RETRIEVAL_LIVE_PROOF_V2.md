# CML Retrieval v0.1.1 live proof

This temporary pull request validates the deployed protected workflow after the privacy and reconciliation hardening.

The change mentions GitHub Actions workflow permissions, exact pull-request base SHA execution, deterministic memory ranking, public privacy filtering, duplicate managed-comment reconciliation, and exact-attempt evidence.

Acceptance is external to this document: one bot-authored `Relevant CML Memory` comment, no hidden corpus counts or rejected details, and one privacy-redacted retrieval artifact. This branch will be closed without merge after validation.
