# CML Retrieval live proof v4

This temporary pull request validates the deployed retrieval workflow from current `main` commit `df3bc2cd35fdb8eacf99c2f390b5d69666651956`.

The change references GitHub Actions workflow permissions, exact-base execution, deterministic memory ranking, public privacy filtering, managed-comment reconciliation, exact-attempt evidence, and privacy-safe failure stages.

Acceptance: one bot-authored `Relevant CML Memory` comment, a successful retrieval run, and a privacy-redacted artifact. The branch will be closed without merge after validation.
