# CML Retrieval deployed live proof

This temporary pull request validates the deployed retrieval workflow from current `main` commit `c684257d675a6252bc4a9d6a4e56f65a6d03cd13`.

The change references GitHub Actions workflow permissions, exact base SHA execution, deterministic memory ranking, public privacy filtering, managed-comment reconciliation, and exact-attempt evidence.

Acceptance: one bot-authored `Relevant CML Memory` comment, a successful retrieval run, and a privacy-redacted artifact. This branch will be closed without merge after validation.
