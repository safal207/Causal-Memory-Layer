# Tradernet mobile public-QA causal memory

This public Memory Pack preserves the causal relationship between three independently observed mobile-only defects without turning repetition into false certainty.

## Remembered facts

- Mobile user-agents receive a 404 experience on the public chart route.
- Late discovery of the mobile hero image materially increases LCP.
- The mobile terminal login downloads a hidden 346,800-byte 2x image.
- Terminal entry also requests a missing first-party asset.

## Bounded causal hypothesis

The repeated mobile-only pattern supports a possible shared device-routing or responsive-resource branch with confidence `75/100`. The Memory Pack marks this node as `proposed`, not verified.

The pack therefore remembers two different things separately:

1. four findings are independently supported for human reporting;
2. one shared root cause remains unproven and requires controlled experiments.

## Selected path

```text
repeated mobile-only findings
→ proposed shared mobile branch
→ controlled branch experiments
→ preserve unknowns
→ bounded cluster lesson
```

## Authority boundary

The pack is public, contains no private data, and grants no execution or merge authority. Retrieval of this memory cannot approve an external report or convert the shared-cause hypothesis into fact.

Machine-readable pack: `examples/memory_packs/tradernet_mobile_public_qa_v1.json`

Pack ID: `f5bac00f1be20c9ca5717236b0e5d0b2c433bc8ff780101736fa267e1c097609`
